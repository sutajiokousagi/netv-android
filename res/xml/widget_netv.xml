<?xml version="1.0" encoding="utf-8"?>

<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minHeight="72dip"
    android:minWidth="72dip"
    android:initialLayout="@layout/widget_netv"
    android:updatePeriodMillis="3600000"
	/>
    
    <!-- The update period of 1 hour is the last resort to keep the widget UI
    in sync with SharePreferences. It is normally not needed.
     --> 
    
    <!-- Android 3.0 onwards
    android:previewImage="@drawable/sms_on"
     -->        